import React from 'react'
import {Link} from 'react-router'
import PropTypes from 'prop-types';

const Home = (props) => {

  return (
    <section>
      <h1>Home</h1>
    </section>
  )
}

export default (Home)
